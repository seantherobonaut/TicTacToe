const Board = function(sizeX=0, sizeY=0)
{
    checkInstance(this, Board);
    //TODO write checks for constructor ints

    let board = new Array();

    this.getState = function()
    {

    };

    console.log("Board was created!");
};